{
  "name": "the-collab",
  "version": "1.0.0",
  "private": true,
  "description": "Co.LLab web app — static site + Netlify Functions backend for .LLab product entitlements and delivery.",
  "scripts": {
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0",
    "stripe": "^17.4.0"
  },
  "devDependencies": {
    "playwright": "^1.56.1"
  }
}
